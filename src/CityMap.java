// CityMap.java
// Module 1: City Map & Environment
// Responsible for loading and managing the city graph (adjacency list)

// CityMap.java
// Module 1: City Map & Environment
// Responsible for loading and managing the city graph (adjacency list)

// Expected Output Example:
// - Successfully loads city map from file
// - Provides neighbors and edge weights for nodes
// - Example usage:
//   CityMap map = new CityMap("city_map.csv");
//   List<Integer> neighbors = map.getNeighbors(5);
//   int weight = map.getEdgeWeight(5, 7);
import java.util.List;

public class CityMap {
    // TODO: Implement graph structure and methods
    public CityMap(String mapFile) {
        // Load city map from file
    }
    // Add methods: getNeighbors, getEdgeWeight, etc.
}
